[pytest]
testpaths = tests
python_files = test_*.py
addopts = -m "not slow"
markers =
    live: needs the live SQL Server instances; skipped when unreachable
    slow: minutes-long full-table pulls against the CMMS; opt in with -m slow
